services:
  ide:
    build: .
    ports:
      - "${IDE_PORT:-8765}:${IDE_PORT:-8765}"
    env_file:
      - .env
    environment:
      IDE_HOST: 0.0.0.0
    volumes:
      - ./workspace:/workspace
    working_dir: /workspace
    command: ["python", "-m", "ide_core.server"]
